Open Terminal -- ot
This is a small script used to manage ssh connection (primarily with virtualbox) with terminator terminals.
The goal is to increase the functionality of terminator by allowing the user to quickly open n number of terminals (splitted or joined with three different patterns) and direcly ssh into other machines.
